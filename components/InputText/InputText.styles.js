import { StyleSheet } from "react-native";

export default StyleSheet.create({
  Input: {
    height: 40,
    backgroundColor: "#fff",
    borderColor: "#ccc",
    borderWidth: 1,
    paddingHorizontal: 10,
    borderRadius: 5,
    marginVertical: 10,
    borderRadius: 5,
  },
});
